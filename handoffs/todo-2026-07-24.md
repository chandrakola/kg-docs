# TODO — 2026-07-24

Compiled 2026-07-23 evening from the explorer-migration + invite-gate-removal review sessions.
Grouped by priority. Each item notes *why* and *where* so you can pick up cold.

Status of the two review loops as of tonight:
- **Explorer migration** — committed, series closed (Claude rounds 1–9).
- **Invite-gate removal** — committed on a clean tree, series closed (Claude rounds 1–3).
Nothing below is blocking a loop; these are the follow-ups those loops surfaced.

---

## P1 — Do first (small, unblock other things)

- [x] **Write `docs/deployment-model.md`** capturing decisions D-1…D-6 (sandbox is the
      demo/deploy target, home-lab stays dev over Tailscale, invite gate deleted, private
      GitHub + Docker Hub as delivery bridges, CIDR fallback). Confirmed *not written yet*.
      Source: `docs/rwdkn-explorer-migration-review-feedback-claude-round-6.md` §1.
      *Why first:* three existing docs still describe the superseded model, and every
      deploy task below assumes these decisions — they need one authoritative home.

- [x] **Fix `logs/` permission bug (M3).** `logs/` is still `root:root`, so
      `subprojects/query-app/tests/` can't even collect (`PermissionError` on
      `query_app.py`'s import-time `FileHandler`). Either `sudo chown -R $USER:$USER logs/`
      or make the log handler lazy / fall back to a writable path. Pre-existing (first seen
      in migration round 4); its own small fix, not part of either review series.
      *Why:* the query-app suite is uncollectable for any non-root run right now.

## P2 — Deploy path to the NLM sandbox (do before any sandbox deploy)

- [x] **Automate the image push.** No `docker push` exists in `deploy/rwdkn/Jenkinsfile.rwdkn`
      — the deploy compose *consumes* `docker.io/chandrakola/real-kg-*` images but nothing
      publishes them. If images are pushed by hand today, that manual step *is* the bridge;
      automate it first.

- [x] **Pin `IMAGE_TAG`; drop the `latest` default.** Round 9 flagged this; the
      `${IMAGE_TAG:-latest}` default is no longer present in
      `deploy/rwdkn/docker-compose.rwdkn.yml` (0 occurrences) — **verify it was replaced with
      an immutable tag, not just removed.** Rollback + blue/green need a real version.

- [x] **Decide the sandbox secrets mechanism.** Infisical (`real-kg-ww-mv`) almost certainly
      isn't reachable/approved from the NIH sandbox. Neo4j/Postgres creds need another path.
      Answer this *before* the first deploy attempt, not during.

- [ ] **Confirm the real NIH egress IP** before relying on the Cloudflare CIDR fallback.
      ARIN allocations ≠ actual egress. Have someone on the NIH network hit an echo endpoint
      and report the observed source IP; cover IPv6 or drop the AAAA record. Only matters if
      the CIDR-at-Cloudflare fallback (D-6) is used.

- [ ] **Scope delivery credentials read-only + track expiry.** GitHub repo-scoped *deploy
      key* (not a personal PAT), Docker Hub *read-only* access token. Record expiry dates
      somewhere visible — token expiry fails silently, months later, in the env you can debug
      least. (Docker Hub paid tier already confirmed to cover the 4 private repos.)

## P3 — Cleanup / hygiene (nice to have, non-blocking)

- [x] **Strengthen the deny-map comment** in `docker/edge.nginx.conf.template`. The kept
      root-route allowlist (`/nlq`, `/insight`, …) is only safe if whatever fronts the stack
      enforces Access upstream. Turn the existing "Cloudflare Access-gated" note into an
      explicit *requirement*: "no edge-local auth; any public exposure MUST enforce Access
      upstream." Source: invite-gate-removal review round 3, §2.

- [x] **Reconcile the superseded architecture docs** with `deployment-model.md` once written:
      `docs/specs/real-kg-edge-gateway.md` (describes cloudflared rwdkn doesn't run + the
      retired invite/QR phase) and `docs/nlmkn-federation-restructure-plan.md` (no mention of
      an NLM-hosted environment).

- [ ] **Commit-message hygiene going forward:** recent commits label config/code changes as
      `docs:` (e.g. `84bbaa4`). Since this repo's git history is the audit trail for the
      review loops, use `fix:`/`refactor:` for non-doc changes. (Past commits — leave; just a
      going-forward note.)

## P4 — Tooling to evaluate (from tonight; no rush, don't spin up idle)

- [ ] **Dify** (github.com/langgenius/dify) — evaluate *only* against a concrete goal: a
      polished chat/RAG interface over the knowledge graph for NLM reviewers. Fits the
      existing Postgres/Docker lab. Note: license is Apache-based *with added conditions*
      (SaaS resale / branding) — read before anything beyond internal/demo use.

- [ ] **agentmemory** (github.com/rohitg00/agentmemory) — only worth trialing if the current
      file-based memory is hitting limits, or if you want memory shared across other agents
      (Cursor/Copilot). If trialed: local embeddings only, scratch project, nothing sensitive
      out. README metrics are self-reported — test on your own data.

- [ ] **awesome-claude-code** (github.com/hesreallyhim/awesome-claude-code) — browse the
      Multi-Agent Orchestration, Skills & Memory, and Observability sections against your
      Gemini/Claude review loop. Treat every runnable item (hooks, MCP, skills) as
      untrusted-until-read.

---

### Notes
- Both review series are **done and committed** — no open review round to return to.
- Claude reviewer-role convention is in memory (`feedback_claude_reviewer_role`): reviewer
  writes `<slug>-review-feedback-claude-round-N.md`, exchange-numbered, dated header, 5-round
  diminishing-returns checkpoints, hard cap 20.
- Standing limitation on everything deploy-related: the `deploy/rwdkn/` SSO stack and the NLM
  sandbox are unreachable from the home lab, so those paths are verified by config, never run.
